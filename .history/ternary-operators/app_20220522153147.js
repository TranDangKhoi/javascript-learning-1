//Ternary Operators (cách viết if else rút gọn)
const yourAge = 18;
let msg = "";
if (yourAge >= 18) {
    msg = "You are an adult";
} else {
    msg = "Your are still young";
}
// Condition ? do something : do something else

let msg2 = yourAge >= 18 ? msg = "You're an adult" : msg = "Your are still young"